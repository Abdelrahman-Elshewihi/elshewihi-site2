# لوحة التحكم (Admin Dashboard) — دليل التفعيل خطوة بخطوة

الموقع بقى فيه لوحة تحكم خاصة بيك بس، تقدر منها تضيف/تعدّل/تمسح المشاريع (الصورة، العنوان، الوصف، السعر...)
من غير ما تفتح الكود خالص. اللوحة موجودة على الرابط: **yourdomain.com/admin**

اللوحة دي مبنية على أداة اسمها **Decap CMS** (أداة مجانية ومستخدمة عالميًا في آلاف المواقع، مش حاجة مبنية من الصفر
وممكن تبوظ — دي معتمدة ومجربة). بتشتغل بالطريقة دي: إنت بتعدّل من اللوحة → التعديل بيتسجل تلقائيًا في مستودع
الموقع على GitHub → الموقع بيعيد نشر نفسه تلقائيًا خلال دقيقة أو اتنين.

**التفعيل محتاج مرة واحدة بس الخطوات دي (كلها مجانية):**

## 1) ارفع الموقع على GitHub
1. اعمل حساب على github.com لو مفيش
2. اعمل Repository جديد (خليه Private لو حابب)
3. ارفع فيه كل ملفات المجلد ده زي ما هي (بنفس ترتيب الفولدرات: index.html, images/, data/, admin/)

## 2) اربط الـRepository بـNetlify
1. روح netlify.com → سجل دخول بحساب GitHub بتاعك
2. "Add new site" → "Import an existing project" → اختار الـRepository اللي عملته
3. سيب الإعدادات زي ما هي واضغط Deploy — الموقع هيشتغل على رابط زي `xxxx.netlify.app`

## 3) فعّل نظام الدخول (Netlify Identity)
1. من داشبورد الموقع في Netlify → **Site configuration → Identity → Enable Identity**
2. تحت "Registration preferences" اختار **Invite only** (عشان محدش يقدر يعمل حساب غيرك)
3. من نفس الصفحة فعّل **Git Gateway** (موجودة تحت Identity → Services)

## 4) ادعُ نفسك كمستخدم/أدمن
1. من نفس صفحة Identity، دوس **Invite users** واكتب الإيميل بتاعك
2. هيوصلك إيميل من Netlify فيه رابط تفعيل الحساب — افتحه واعمل باسورد

## 5) عدّل رابط الموقع في ملف الإعدادات
افتح `admin/config.yml` وغيّر السطرين دول لرابط موقعك الحقيقي بعد ما تاخده من Netlify:
```
site_url: https://xxxx.netlify.app
display_url: https://xxxx.netlify.app
```
ارفع التعديل على GitHub (هيعيد النشر تلقائي).

## 6) خلاص — ادخل لوحتك
روح على `yourdomain.com/admin`، سجل دخول بالإيميل والباسورد اللي عملته، وهتلاقي قسم
"المشاريع" فيه كل مشروع بحقوله (صورة، عنوان عربي/إنجليزي، وصف، سعر...) قابل للتعديل من غير أي كود.

---

### لو مش عايز تعمل الخطوات دي دلوقتي
مفيش أي مشكلة — الموقع شغال عادي بردو، والتعديل هيفضل بنفس الطريقة القديمة (تعديل ملف
`data/projects.json` مباشرة برفعه على الاستضافة). بس أنصحك تعمل الخطوات دي لو عايز تعدّل بسهولة
من الموبايل أو من غير ما تفتح كود خالص، زي ما طلبت بالظبط.

### ملاحظة مهمة
دومين الأدمن ده مش عام — محدش يقدر يدخله أو يعدّل حاجة غيرك إنت، لأن التسجيل مقفول على "Invite only".
